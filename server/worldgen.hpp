#include "Brick.hpp"


namespace worldgen{
    void generate(std::vector<Brick>* bricks){ // Pointer to the bricks vector. It'll be empty when you use it, I promise. (It'll be spliced into the world array; the vector is for niceness).
        // Worldgen people: this is the function
        // Do whatever else you want in the namespace but please don't change this function profile as it's set up to be C++ey.
    }
}
